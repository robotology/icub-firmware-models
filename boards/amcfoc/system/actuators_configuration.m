%% ------------------------------------------------------------------
%  You can modify the values of the fields in ActuatorInitConfMultiple
%  and evaluate this cell to create/update this structure
%  in the MATLAB base workspace.
% -------------------------------------------------------------------

%% CONFIGURATION FOR FIRST MOTOR

ActuatorInitConfMultiple(1).thresholds = struct;
ActuatorInitConfMultiple(1).thresholds.jntVelMax = single(40000);
ActuatorInitConfMultiple(1).thresholds.motorNominalCurrents = single(0.35);
ActuatorInitConfMultiple(1).thresholds.motorPeakCurrents = single(0.7);
ActuatorInitConfMultiple(1).thresholds.motorOverloadCurrents = single(1);
ActuatorInitConfMultiple(1).thresholds.motorPwmLimit = uint32(0);
ActuatorInitConfMultiple(1).thresholds.motorCriticalTemperature = single(0);
ActuatorInitConfMultiple(1).pids = struct;
ActuatorInitConfMultiple(1).pids.currentPID = struct;
ActuatorInitConfMultiple(1).pids.currentPID.type = ControlModes.Current;
ActuatorInitConfMultiple(1).pids.currentPID.OutMax = single(0);
ActuatorInitConfMultiple(1).pids.currentPID.OutMin = single(0);
ActuatorInitConfMultiple(1).pids.currentPID.P = single(2);
ActuatorInitConfMultiple(1).pids.currentPID.I = single(500);
ActuatorInitConfMultiple(1).pids.currentPID.D = single(0);
ActuatorInitConfMultiple(1).pids.currentPID.N = single(10);
ActuatorInitConfMultiple(1).pids.currentPID.I0 = single(0);
ActuatorInitConfMultiple(1).pids.currentPID.D0 = single(0);
ActuatorInitConfMultiple(1).pids.currentPID.shift_factor = uint8(0);
ActuatorInitConfMultiple(1).pids.velocityPID = struct;
ActuatorInitConfMultiple(1).pids.velocityPID.type = ControlModes.Velocity;
ActuatorInitConfMultiple(1).pids.velocityPID.OutMax = single(0);
ActuatorInitConfMultiple(1).pids.velocityPID.OutMin = single(0);
ActuatorInitConfMultiple(1).pids.velocityPID.P = single(-3e-5);
ActuatorInitConfMultiple(1).pids.velocityPID.I = single(-3e-5);
ActuatorInitConfMultiple(1).pids.velocityPID.D = single(0);
ActuatorInitConfMultiple(1).pids.velocityPID.N = single(10);
ActuatorInitConfMultiple(1).pids.velocityPID.I0 = single(0);
ActuatorInitConfMultiple(1).pids.velocityPID.D0 = single(0);
ActuatorInitConfMultiple(1).pids.velocityPID.shift_factor = uint8(0);
ActuatorInitConfMultiple(1).pids.positionPID = struct;
ActuatorInitConfMultiple(1).pids.positionPID.type = ControlModes.Position;
ActuatorInitConfMultiple(1).pids.positionPID.OutMax = single(0);
ActuatorInitConfMultiple(1).pids.positionPID.OutMin = single(0);
ActuatorInitConfMultiple(1).pids.positionPID.P = single(0);
ActuatorInitConfMultiple(1).pids.positionPID.I = single(0);
ActuatorInitConfMultiple(1).pids.positionPID.D = single(0);
ActuatorInitConfMultiple(1).pids.positionPID.N = single(0);
ActuatorInitConfMultiple(1).pids.positionPID.I0 = single(0);
ActuatorInitConfMultiple(1).pids.positionPID.D0 = single(0);
ActuatorInitConfMultiple(1).pids.positionPID.shift_factor = uint8(0);
ActuatorInitConfMultiple(1).motor = struct;
ActuatorInitConfMultiple(1).motor.has_hall_sens = true;
ActuatorInitConfMultiple(1).motor.has_quadrature_encoder = false;
ActuatorInitConfMultiple(1).motor.has_speed_quadrature_encoder = false;
ActuatorInitConfMultiple(1).motor.has_torque_sens = false;
ActuatorInitConfMultiple(1).motor.use_index = false;
ActuatorInitConfMultiple(1).motor.enable_verbosity = false;
ActuatorInitConfMultiple(1).motor.hall_sensors_offset = int16(0);
ActuatorInitConfMultiple(1).motor.rotor_encoder_resolution = int16(0);
ActuatorInitConfMultiple(1).motor.rotor_index_offset = int16(0);
ActuatorInitConfMultiple(1).motor.encoder_tolerance = uint8(0);
ActuatorInitConfMultiple(1).motor.pole_pairs = uint8(7);
ActuatorInitConfMultiple(1).motor.Kbemf = single(0);
ActuatorInitConfMultiple(1).motor.Rphase = single(0);
ActuatorInitConfMultiple(1).motor.Imin = single(0);
ActuatorInitConfMultiple(1).motor.Imax = single(0);
ActuatorInitConfMultiple(1).motor.Vcc = single(24);
ActuatorInitConfMultiple(1).motor.Vmax = single(44);
ActuatorInitConfMultiple(1).motor.resistance = single(25.9);
ActuatorInitConfMultiple(1).motor.inductance = single(271);
ActuatorInitConfMultiple(1).motor.thermal_resistance = single(16);
ActuatorInitConfMultiple(1).motor.thermal_time_constant = single(797.5);

%% CONFIGURATION FOR SECOND MOTOR

ActuatorInitConfMultiple(2).thresholds = struct;
ActuatorInitConfMultiple(2).thresholds.jntVelMax = single(40000);
ActuatorInitConfMultiple(2).thresholds.motorNominalCurrents = single(0.35);
ActuatorInitConfMultiple(2).thresholds.motorPeakCurrents = single(0.7);
ActuatorInitConfMultiple(2).thresholds.motorOverloadCurrents = single(2);
ActuatorInitConfMultiple(2).thresholds.motorPwmLimit = uint32(0);
ActuatorInitConfMultiple(2).thresholds.motorCriticalTemperature = single(0);
ActuatorInitConfMultiple(2).pids = struct;
ActuatorInitConfMultiple(2).pids.currentPID = struct;
ActuatorInitConfMultiple(2).pids.currentPID.type = ControlModes.Current;
ActuatorInitConfMultiple(2).pids.currentPID.OutMax = single(0);
ActuatorInitConfMultiple(2).pids.currentPID.OutMin = single(0);
ActuatorInitConfMultiple(2).pids.currentPID.P = single(2);
ActuatorInitConfMultiple(2).pids.currentPID.I = single(500);
ActuatorInitConfMultiple(2).pids.currentPID.D = single(0);
ActuatorInitConfMultiple(2).pids.currentPID.N = single(10);
ActuatorInitConfMultiple(2).pids.currentPID.I0 = single(0);
ActuatorInitConfMultiple(2).pids.currentPID.D0 = single(0);
ActuatorInitConfMultiple(2).pids.currentPID.shift_factor = uint8(0);
ActuatorInitConfMultiple(2).pids.velocityPID = struct;
ActuatorInitConfMultiple(2).pids.velocityPID.type = ControlModes.Velocity;
ActuatorInitConfMultiple(2).pids.velocityPID.OutMax = single(0);
ActuatorInitConfMultiple(2).pids.velocityPID.OutMin = single(0);
ActuatorInitConfMultiple(2).pids.velocityPID.P = single(-3e-5);
ActuatorInitConfMultiple(2).pids.velocityPID.I = single(-3e-5);
ActuatorInitConfMultiple(2).pids.velocityPID.D = single(0);
ActuatorInitConfMultiple(2).pids.velocityPID.N = single(10);
ActuatorInitConfMultiple(2).pids.velocityPID.I0 = single(0);
ActuatorInitConfMultiple(2).pids.velocityPID.D0 = single(0);
ActuatorInitConfMultiple(2).pids.velocityPID.shift_factor = uint8(0);
ActuatorInitConfMultiple(2).pids.positionPID = struct;
ActuatorInitConfMultiple(2).pids.positionPID.type = ControlModes.Position;
ActuatorInitConfMultiple(2).pids.positionPID.OutMax = single(0);
ActuatorInitConfMultiple(2).pids.positionPID.OutMin = single(0);
ActuatorInitConfMultiple(2).pids.positionPID.P = single(0);
ActuatorInitConfMultiple(2).pids.positionPID.I = single(0);
ActuatorInitConfMultiple(2).pids.positionPID.D = single(0);
ActuatorInitConfMultiple(2).pids.positionPID.N = single(0);
ActuatorInitConfMultiple(2).pids.positionPID.I0 = single(0);
ActuatorInitConfMultiple(2).pids.positionPID.D0 = single(0);
ActuatorInitConfMultiple(2).pids.positionPID.shift_factor = uint8(0);
ActuatorInitConfMultiple(2).motor = struct;
ActuatorInitConfMultiple(2).motor.has_hall_sens = true;
ActuatorInitConfMultiple(2).motor.has_quadrature_encoder = false;
ActuatorInitConfMultiple(2).motor.has_speed_quadrature_encoder = false;
ActuatorInitConfMultiple(2).motor.has_torque_sens = false;
ActuatorInitConfMultiple(2).motor.use_index = false;
ActuatorInitConfMultiple(2).motor.enable_verbosity = false;
ActuatorInitConfMultiple(2).motor.hall_sensors_offset = int16(0);
ActuatorInitConfMultiple(2).motor.rotor_encoder_resolution = int16(0);
ActuatorInitConfMultiple(2).motor.rotor_index_offset = int16(0);
ActuatorInitConfMultiple(2).motor.encoder_tolerance = uint8(0);
ActuatorInitConfMultiple(2).motor.pole_pairs = uint8(7);
ActuatorInitConfMultiple(2).motor.Kbemf = single(0);
ActuatorInitConfMultiple(2).motor.Rphase = single(0);
ActuatorInitConfMultiple(2).motor.Imin = single(0);
ActuatorInitConfMultiple(2).motor.Imax = single(0);
ActuatorInitConfMultiple(2).motor.Vcc = single(24);
ActuatorInitConfMultiple(2).motor.Vmax = single(44);
ActuatorInitConfMultiple(2).motor.resistance = single(25.9);
ActuatorInitConfMultiple(2).motor.inductance = single(271);
ActuatorInitConfMultiple(2).motor.thermal_resistance = single(16);
ActuatorInitConfMultiple(2).motor.thermal_time_constant = single(797.5);
